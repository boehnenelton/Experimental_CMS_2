New page button on the pages tab is invisible 

Edit on the category table doesn't work 
------
On the built-site, sidebar links don't work 

Main CSS body on the exported side needs a lot of work 

We need to refine an optimizing go through the CSS on them main website that's exported and double-check everything